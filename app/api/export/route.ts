import { NextRequest,NextResponse } from "next/server"; import * as XLSX from "xlsx"; import { listJobs } from "@/lib/jobs";
export const runtime="nodejs";
export function GET(req:NextRequest){const fmt=req.nextUrl.searchParams.get("format")||"csv"; const rows=listJobs().map(j=>({"Job ID":j.job_id,Company:j.company,Role:j.title,"Match Score":j.score,Location:j.location,"Work Model":j.work_model,Posted:j.posted_at,Found:j.found_at,"Canonical Source":j.canonical_source,ATS:j.ats,"Material Status":j.material_status,"Review Status":j.review_status,"Application Status":j.application_status,Priority:j.priority,Notes:j.notes}));if(fmt==="xlsx"){const book=XLSX.utils.book_new();XLSX.utils.book_append_sheet(book,XLSX.utils.json_to_sheet(rows),"Jobs");return new NextResponse(XLSX.write(book,{type:"buffer",bookType:"xlsx"}),{headers:{"Content-Type":"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet","Content-Disposition":"attachment; filename=job-agent-export.xlsx"}})}const csv=XLSX.utils.sheet_to_csv(XLSX.utils.json_to_sheet(rows));return new NextResponse(csv,{headers:{"Content-Type":"text/csv","Content-Disposition":"attachment; filename=job-agent-export.csv"}})}
