import { listJobs } from "@/lib/jobs"; import { db } from "@/lib/db";
export const dynamic="force-dynamic";
const badge=(v:string)=> <span className="badge">{v.replaceAll("_"," ")}</span>;
export default function Home(){const jobs=listJobs(),runs=db().prepare("SELECT * FROM discovery_runs ORDER BY id DESC LIMIT 5").all() as any[],pending=jobs.filter(j=>j.review_status==="PENDING"),interested=jobs.filter(j=>j.review_status==="INTERESTED"),ready=interested.filter(j=>j.material_status==="READY"),apply=jobs.filter(j=>j.application_status==="READY_TO_APPLY");const section=(title:string,items:any[],empty:string)=><section className="queue"><h2>{title} <small>{items.length}</small></h2>{items.length?<div className="table"><div className="row head"><span>ID</span><span>Company / role</span><span>Match</span><span>Location</span><span>Source</span><span>Review</span><span>Next step</span></div>{items.map(j=><a className="row" href={`/jobs/${j.job_id}`} key={j.job_id}><span>{j.job_id}</span><span><b>{j.company}</b><small>{j.title}</small></span><span>{j.score==null?"-":`${j.score}/100`}</span><span>{j.location||"-"}</span><span>{j.canonical_source}</span><span>{badge(j.review_status)}</span><span>{j.material_status==="READY"?badge(j.application_status):"Review materials"}</span></a>)}</div>:<p className="muted">{empty}</p>}</section>;return <main><section className="hero"><div><p className="eyebrow">YOUR LOCAL WORKSPACE</p><h1>Search. Review. Tailor. Apply.</h1><p className="muted">Discovery is bounded and deterministic. Interest, materials, and every application decision remain yours.</p></div><form action="/api/discover" method="post"><button>Run Discovery</button></form></section><section className="setup"><b>Private setup</b><span>Add a factual profile before tailoring. Generated materials contain no invented facts.</span><a href="/api/setup">View checklist</a></section><section className="metrics"><Metric n={pending.length} t="Awaiting review"/><Metric n={interested.length} t="Interested"/><Metric n={ready.length} t="Materials ready"/><Metric n={apply.length} t="Ready to apply"/></section>{section("1. Review new jobs",pending,"Run discovery to add jobs for review.")}{section("2. Tailor interested jobs",interested,"Mark a reviewed job Interested to prepare materials.")}{section("3. Apply manually",ready,"Materials will appear here after deterministic generation.")}<section className="toolbar"><h2>Exports</h2><a className="secondary" href="/api/export?format=xlsx">Export XLSX</a><a className="secondary" href="/api/export?format=csv">Export CSV</a></section><section className="runs"><h2>Discovery runs</h2>{runs.length?runs.map(r=><p key={r.id}><b>{r.ended_at?"Completed":"Running"}</b> · {r.sources_attempted||"No sources configured"} · {r.new_jobs} new / {r.duplicates} duplicates {r.errors&&<em> · {r.errors}</em>}</p>):<p className="muted">No discovery runs yet.</p>}</section></main>}
function Metric({n,t}:{n:number;t:string}){return <div className="metric"><b>{n}</b><span>{t}</span></div>}
