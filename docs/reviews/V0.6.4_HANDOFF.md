# V0.6.4 Handoff

## Release context

- Released baseline: `v0.6.3`, commit `b87c38d7254a5e65d80c4ab93e507dfc00fd385d`.
- Feature branch: `codex/v0.6.4`.
- Implementation commit: `b9806be3d910ca693a10d773a299b715d8153cde` (`fix: resolve V0.6.4 workflow defects`).

## Delivered scope

- Resolves an exact persisted `job_sources.url` to its existing permanent job before external-ID, secondary-identity, JD-hash, or creation logic can allocate a duplicate job.
- Preserves permanent job identity, source history, canonical-source selection, V0.6.3 BA substantive rediscovery upgrades, and richer existing captured content.
- Gives every application-status option its actual enum value and represents persisted `NOT_APPLIED`, so `READY_TO_APPLY` requires an explicit human selection and persists through POST and reload.
- Aligns the Materials ready dashboard wording with the manual application-status selector.
- Adds focused URL-identity and complete `NOT_APPLIED` → `READY_TO_APPLY` persistence/queue regression coverage, plus the Vitest alias needed to render the existing Next.js page in tests.
- Records the Phase 0 Ponytail and mandatory C2C workflow requirements in `AGENTS.md` and updates patch-version metadata/changelog.

## Changed files

- `AGENTS.md`
- `app/jobs/[id]/page.tsx`
- `app/page.tsx`
- `docs/CHANGELOG.md`
- `lib/jobs.ts`
- `package.json`
- `package-lock.json`
- `tests/v064-regressions.test.ts`
- `vitest.config.ts`
- `docs/reviews/V0.6.4_HANDOFF.md`

## Validation

- Focused V0.6.4, V0.6.3 discovery-quality, and V0.6 workflow tests: 23 passed across 3 files.
- Full test suite: 107 passed across 8 files.
- Typecheck: passed.
- Lint: passed.
- Production build: passed. The existing dynamic local-filesystem tracing warnings in `lib/jobs.ts` and `lib/materials.ts` remain non-failing.

## Production-style verification

- Used the existing private local configuration with a process-local BA key and an isolated temporary database/storage directory; no key or private data was committed.
- First real discovery: 16 upstream, 14 filtered, 2 accepted/new, 0 failures. Second discovery against the same isolated database: 2 duplicates, 0 new, 0 failures.
- Changed company, title, location, and external ID while rediscovering a known URL: permanent ID remained `JOB-0001`, job count remained 2, source count remained 1, canonical URL/source remained valid, substantive content remained substantive, and richer captured content was unchanged.
- Set an isolated substantive job with READY materials to `NOT_APPLIED`, verified the page selected `NOT_APPLIED`, submitted the actual `READY_TO_APPLY` enum value through the route, received the expected 303 redirect, verified persisted and re-rendered `READY_TO_APPLY`, and confirmed the queue moved from Materials ready to Ready to Apply.
- No external application was submitted and no browser/application automation was introduced.
- The isolated verification directory and local validation transcript were removed after use; unrelated private user data was not changed.

## Independent review

- Mandatory `codex-with-chatgpt` review completed with `STATE: DONE` after inspecting the actual workspace, `codex/v0.6.4` diff from `b87c38d`, relevant implementation/tests, and fresh recorded validation output.
- The reviewer independently confirmed URL resolution occurs before job creation; permanent identity, source/canonical state, BA upgrades, and richer content are preserved; application enum submission, `NOT_APPLIED` rendering, persistence/reload, validation, and queue movement are correct; and no external application behavior or scope expansion was introduced.
- Reviewer verdict: V0.6.4 is implementation/release-ready with no product or code blocker.

## Remaining limitations and non-goals

- The pre-existing build tracing warnings remain unchanged.
- Provider expansion, provider metrics, scoring/Germany-filter/tailoring redesign, resume changes, runtime AI, application automation, broad UI redesign, and V0.7 functionality remain out of scope.

## Safety and readiness

- `main` was not modified or merged, the released `v0.6.3` tag was not changed, and no V0.6.4 tag or release was created.
- Private profile, runtime database, real-discovery artifacts, and credentials remain out of Git.
- The branch is ready for final human release review.
