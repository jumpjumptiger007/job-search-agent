# V0.5 Handoff

## Release context

- Product baseline: `v0.4.0` / `d361dde0e3f3e33b6e89002b3520759bc75821f6`.
- Branch starting point: current `main` at `e934c471fe1cc3ab5aa1c296d036d77be3f2c7fa`, including the post-V0.4 handoff workflow update.
- V0.5 branch: `codex/v0.5-search-quality`.
- Implementation commits: `8dd3e4c559f78b44b38482a83829d349f62def08`, pre-review hardening fix `f0afc471151f30bf61923866ff56876ef71a15c0`, deep-audit edge-case fix `d97677022267d2b8d86805f3d1eedaba4dc2cb34`, and Germany-focused structured-location fix `52cf58f96530a01db911deb60733564910b7dbfe`.
- Final branch HEAD: this documentation-only handoff update commit (`HEAD`); it is separate from the implementation commits above.

## Delivered scope

- Added a narrow public Personio adapter for explicitly configured sources only. It normalizes jobs into the existing discovery/ingest path, uses `personio:<tenant>:<job-id>` identities, records source tenants, isolates provider and malformed-source failures from other configured providers, accepts only public `<tenant>.jobs.personio.<tld>` forms, skips malformed position records, and keeps normalized job URLs on the configured Personio source.
- Personio discovery now uses the documented public Company Career Site XML feed at `/xml?language=<lang>`, not the unsupported JSON path. XML positions are deterministically normalized from public `id`, `name`, `office`, `jobDescriptions`, and `employmentType` fields.
- Hardened Germany-based eligibility filtering. It rejects only clear US, Canada, UK, or Switzerland-only work; single explicit incompatible locations (including remote-in-a-foreign-country and observed non-European Japan/Australia/Singapore/APAC location labels); explicit foreign role locations; and explicit foreign residency or work-authorisation requirements. Compatible, mixed, and ambiguous listings remain for Review; country mentions alone do not reject a job.
- Made role-family matching title-led. Description-only generic management or operations terms no longer qualify unrelated roles; clear title matches, punctuation/plural variants, and plausible adjacent titles remain available for Review.

No database migration, review-gate change, scoring redesign, material-generation change, browser automation, runtime LLM, or additional ATS support was added.

## Validation

- `npm test`: passed, 70 tests across 3 files.
- `npm run typecheck`: passed.
- `npm run lint`: passed.
- `npm run build`: passed. The existing Turbopack dynamic-local-filesystem tracing warnings in `lib/jobs.ts` and `lib/materials.ts` remain non-failing and unchanged by V0.5.
- Focused production-style deterministic validation: `tests/v05-search-quality.test.ts` passed 40 checks covering Personio XML normalization, supported URL forms, malformed records, tenant/job identity, rediscovery/deduplication, malformed configuration and provider failure isolation, compatible/mixed/ambiguous geography retention, explicit foreign rejection, and generic role-noise rejection while retaining clear and plausible roles.

## Real local production trial

- Ran discovery against the user's existing private source configuration using only a disposable `/private/tmp` database and storage root; no production job data or materials were altered.
- The normal private-preference run attempted three configured public sources and completed with no provider failures, but produced no Review candidates under the user's role filters.
- A second run using the same source configuration with a disposable Germany-only, no-role-family preference profile captured two new jobs with unique external IDs and `PENDING` Review state. The captures were from the existing Greenhouse and Lever paths; no configured Personio job was captured in this sample.
- Repeating that second run against the same disposable database produced two duplicates and no new jobs, confirming real rediscovery/deduplication behavior.
- A bounded public Greenhouse trial exposed three clear non-European structured-location false positives in an initial five-job Germany-filtered sample. After the narrow structured-location fix, the same public trial captured three jobs with no failures; EU-compatible Paris remained eligible while Japan/Australia/Singapore locations were filtered.
- The sample is evidence that configured discovery and the Review boundary remain operational. It is not evidence for a new provider, matching, scoring, CV, or application-automation batch. Personio's live configured source returned no captured sample in this run, so non-empty real-world Personio response compatibility remains covered by deterministic tests rather than this limited trial.
- Verified locally: the documented XML endpoint implementation, realistic XML response-shape tests, deterministic normalization, stable external IDs, same-tenant URLs, provider failure isolation, and deduplication. Not fully live-verified: repeated public tenant XML attempts returned HTTP 429 in this execution environment. Live non-empty Personio validation blocked by remote rate limiting in this execution environment.

## Safety and review status

- All test databases, job folders, temporary configurations, and mocked responses use `/private/tmp` and are removed in test cleanup.
- No profile, CV, runtime database, discovered job, generated material, or other private artifact is staged or committed.
- `main` was not modified; no tag, merge, or release was created.
- The branch is ready for independent review after push.

## Known limitations and deferrals

- Personio support is limited to explicitly configured public `*.jobs.personio.*` sources; there is no tenant discovery, generic employer crawling, or additional ATS framework.
- Geography and authorisation checks deliberately recognize only clear textual contradictions. They are not immigration advice and do not infer citizenship, visa status, or ambiguous location rules.
- Role relevance remains deterministic lexical matching. Broad standalone `management` is intentionally not a qualifying family; standalone `operations` requires a senior operations title. No embeddings, ML, or LLM classification is used.
- The externally supplied `V0.5_ROADMAP.md` and `POST_V0.4_ROADMAP.md` were reviewed after implementation. The V0.5 roadmap confirms the delivered scope; the post-V0.4 roadmap remains a deferred decision framework. The production trial did not demonstrate the evidence threshold for any post-V0.5 batch.
